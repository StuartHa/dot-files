" Number lines relative to current line.
set number relativenumber

" Use two spaces for indenting.
set shiftwidth=2 tabstop=2 expandtab
" Automatic indenting.
set cindent

" Move current line up a line.
map <leader>] :m -2<CR>
" Move current line down a line.
map <leader>[ :m +1<CR>
